{
  "schema": "aipi.semantic-memory.v1",
  "ollama_host": "http://localhost:11434",
  "ollama_model": "bge-m3",
  "dimensions": 1024,
  "rule": "Semantic code search uses local Ollama embeddings. Run `ollama pull bge-m3` before semantic-only search or onboarding rebuild; aipi_callers and aipi_impact continue with exact/lexical fallback when embeddings are unavailable, but onboarding must report semantic memory is OFF loudly instead of silently degrading."
}
