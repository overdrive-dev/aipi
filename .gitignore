node_modules/
npm-debug.log*
*.tgz

# AIPI runtime state (test/dogfood artifacts — never source)
.aipi/runtime/

# Dev-only agent instructions: kept LOCAL in the dev clone so Claude reads it while
# developing the engine, but never shipped into the release checkout clients load.
/CLAUDE.md
